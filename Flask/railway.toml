[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn server:app --workers 1 --threads 4 --worker-class gthread --bind 0.0.0.0:$PORT --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
